#include <iostream>
#include <vector>
#include <string>

using namespace std;


int main() {
    int t, n, k, d, w, up, np; cin >> t;
    while (t--) {
        cin >> n >> k >> d >> w;
        cin >> up;
        while (n--) {
            cin >> np;
        }

    }
    return 0;
}